#!perl
use 5.10.0;
use strict;
use warnings;

use Dist::Iller;

# AUTHORITY
# PODNAME: iller
our $VERSION = '0.1407';

my $iller = Dist::Iller->new;
$iller->parse('before');
$iller->generate_files('before');

system('dzil', @ARGV);

$iller->parse('after');
$iller->generate_files('after');

__END__
